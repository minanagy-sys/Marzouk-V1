Place two brand image files here (referenced by the site):

1. logo.png       — the circular Dr Ahmed Marzouk logo. Used as:
                    • the header logo (mobile + desktop)
                    • the browser/Google favicon (app icons)
                    Recommended: square PNG, >=512x512, transparent or on-brand background.

2. og-doctor.webp  — the doctor's photo used as the default social-share (Open Graph)
                    image for pages without their own. Recommended: 1200x630 JPG.

Commit these into the repo (they deploy with the build) OR upload to the server's
public/brand/ folder. Filenames must match exactly.
